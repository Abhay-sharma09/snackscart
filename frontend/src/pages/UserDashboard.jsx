import React from 'react';

const UserDashboard = () => {
  return (
    <div className="container page-wrapper">
      <h2 style={{ padding: '40px 0 20px' }}>My Dashboard</h2>
      <div className="glass-card" style={{ padding: '20px' }}>
        <p style={{ color: 'var(--text-muted)' }}>User features (Order History, Profile) will be here.</p>
      </div>
    </div>
  );
};

export default UserDashboard;
